import styled from "styled-components";

const Menu = () => {
  return <MenuContainer />;
};

export default Menu;

const MenuContainer = styled.div`
  width: 180px;
  height: 330.56px;
  padding: 12px 16px;
`;
